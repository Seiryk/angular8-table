import { MatrixCalcDirective } from './matrix-calc.directive';

describe('MatrixCalcDirective', () => {
  it('should create an instance', () => {
    const directive = new MatrixCalcDirective();
    expect(directive).toBeTruthy();
  });
});
